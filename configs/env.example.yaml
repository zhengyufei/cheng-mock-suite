mock_server:
  mock: protocol-ministry-platform
  host: 127.0.0.1
  port: 18080
  record_dir: reports/mock-server
  scenario: success

backend:
  base_url: http://127.0.0.1:8000
  receive_path: /api/ministry/receive
  file_path: /api/ministry/file
